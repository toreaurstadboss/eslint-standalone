{
  "name": "eslint-standalone",
  "version": "1.0.0",
  "description": "eslint standalone package with pkg",
  "main": "index.js",
  "bin": "bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tore",
  "license": "MIT",
  "dependencies": {
    "eslint": "^6.6.0"
  }
}
